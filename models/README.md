Model binaries are NOT committed normally.

Future workflow:
  manifest
    ↓
  download script
    ↓
  checksum validation
    ↓
  models/weights/

Mention:
  - license must be recorded
  - source must be recorded
  - checksum should be recorded
  - large binaries remain outside ordinary Git history.